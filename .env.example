# Opcional: token de túnel estável do Cloudflare.
# Se vazio, o compose sobe em modo Quick Tunnel (URL temporária).
CLOUDFLARED_TUNNEL_TOKEN=

# API backend acessada pelo nginx interno do painel2
PANEL_API_UPSTREAM=http://host.docker.internal:8081
PANEL_API_KEY=
